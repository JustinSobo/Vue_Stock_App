import { ITradesQuotesQuery } from "../stocks/trades";
export interface ICryptoTradeInfo {
    conditions: number[];
    exchange: number;
    id: string;
    participant_timestamp: number;
    price: number;
    size: number;
}
export interface ICryptoTrade {
    next_url?: string;
    request_id?: string;
    results?: ICryptoTradeInfo[];
    status?: string;
}
export declare const trades: (apiKey: string, apiBase: string, cryptoTicker: string, query?: ITradesQuotesQuery) => Promise<ICryptoTrade>;
