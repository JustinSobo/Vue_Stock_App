import { IPolygonQuery } from "../transport/request";
import { ITick } from "./ITickJson";
export interface ICryptoDailyOpenCloseQuery extends IPolygonQuery {
    adjusted?: "true" | "false";
}
export interface ICryptoDailyOpenClose {
    close?: number;
    closeingTrades?: ITick;
    day?: string;
    isUTC?: boolean;
    open?: number;
    openTrades?: ITick;
    symbol?: string;
}
export declare const dailyOpenClose: (apiKey: string, apiBase: string, from: string, to: string, date: string, query?: ICryptoDailyOpenCloseQuery) => Promise<ICryptoDailyOpenClose>;
