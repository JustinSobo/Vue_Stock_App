import { ILastTradeInfo } from "./lastTrade";
export interface ILastQuote {
    request_id?: string;
    status?: string;
    results?: ILastTradeInfo;
}
export declare const lastQuote: (apiKey: string, apiBase: string, symbol: string) => Promise<ILastQuote>;
