import { IPolygonQuery } from "../transport/request";
import { IAggsResults } from "./aggregates";
export interface IAggsPreviousCloseQuery extends IPolygonQuery {
    adjusted?: "true" | "false";
}
export interface IAggsPreviousClose {
    ticker?: string;
    adjusted?: boolean;
    queryCount?: number;
    request_id?: string;
    resultsCount?: number;
    status?: string;
    count?: number;
    results?: IAggsResults[];
}
export declare const previousClose: (apiKey: string, apiBase: string, symbol: string, query?: IAggsPreviousCloseQuery) => Promise<IAggsPreviousClose>;
