import { ILastTradeInfo } from "../stocks/lastTrade";
export interface IOptionsLastTrade {
    request_id?: string;
    status?: string;
    results?: ILastTradeInfo;
}
export declare const lastTrade: (apiKey: string, apiBase: string, symbol: string) => Promise<IOptionsLastTrade>;
