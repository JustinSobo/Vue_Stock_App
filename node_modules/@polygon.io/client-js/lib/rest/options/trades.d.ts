import { ITradesQuotesQuery } from "../stocks/trades";
export interface IOptionTradesInfo {
    conditions: number[];
    correction: number;
    exchange: number;
    participant_timestamp: number;
    price: number;
    sip_timestamp: number;
    size: number;
}
export interface IOptionTrades {
    next_url?: string;
    request_id?: string;
    results?: IOptionTradesInfo[];
    status?: string;
}
export declare const trades: (apiKey: string, apiBase: string, optionsTicker: string, query?: ITradesQuotesQuery) => Promise<IOptionTrades>;
